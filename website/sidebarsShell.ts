import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/** Site-owned stubs that are not yet first-class repo docs pages. */
const sidebarsShell: SidebarsConfig = {
  shellSidebar: [
    'components',
    'patterns',
    'get-started',
  ],
};

export default sidebarsShell;
